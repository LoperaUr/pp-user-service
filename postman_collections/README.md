La colección canónica ahora vive en la raíz del monorepo:

- `..\..\postman_collections\plazoleta-pragma.postman_collection.json`

Importa ese archivo en Postman para cargar los endpoints de `pp-user-service` y `pp-food-court-service` en una sola colección.

