server:
  port: 8081
spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true